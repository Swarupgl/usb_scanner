# Optional extra arguments for the udev/systemd USB sanitizer runner.
# One line, appended to the scan command.
# Examples:
#   --allowlist /home/pi/Ai/allowlist.sha256
#   --signed-entropy-override --signed-quarantine-threshold 0.95
#   --risk-threshold 0.6
#   --onnx /home/pi/Ai/pi_models/malconv_finalfinal.onnx
#   --onnx-max-len 1048576
#   --try-ro-mount

